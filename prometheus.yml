# Prometheus configuration for EasyRec-Extended

global:
  scrape_interval: 15s
  evaluation_interval: 15s

scrape_configs:
  - job_name: 'easyrec-extended'
    static_configs:
      - targets: ['app:5000']
    metrics_path: /metrics
